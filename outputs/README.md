# Outputs directory

`figures/` contains reproducible visual outputs. The original exploratory K-means figures are preserved there as tracked historical artifacts. `tables/` is reserved for generated statistical and evaluation tables. `models/` is reserved for generated fitted-model artifacts.

New generated outputs are normally ignored by Git and should be reproducible from the project code and configuration.
